"""Agent build-time configuration — set BACKEND_URL before running pyinstaller."""

# Change this to your production URL before building the .exe
BACKEND_URL = "http://localhost:8000"

APP_NAME    = "Developer Activity"
APP_VERSION = "1.0.0"
